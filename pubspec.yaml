name: password_manager
description: Une application simple de gestion de mots de passe.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.2
  path: ^1.8.3
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_slidable: ^3.0.1
  intl: ^0.19.0
  crypto: ^3.0.3
  random_string: ^2.3.1
  local_auth: ^2.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true